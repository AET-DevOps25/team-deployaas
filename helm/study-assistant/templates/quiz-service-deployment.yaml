apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "study-assistant.fullname" . }}-quiz-service
  labels:
    {{- include "study-assistant.labels" . | nindent 4 }}
    app.kubernetes.io/component: quiz-service
spec:
  replicas: {{ .Values.quizService.replicaCount }}
  selector:
    matchLabels:
      {{- include "study-assistant.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: quiz-service
  template:
    metadata:
      labels:
        {{- include "study-assistant.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: quiz-service
    spec:
      containers:
        - name: quiz-service
          image: "{{ .Values.quizService.image.repository }}:{{ .Values.quizService.image.tag }}"
          imagePullPolicy: {{ .Values.quizService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.quizService.service.targetPort }}
              protocol: TCP
          env:
            {{- toYaml .Values.quizService.env | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/quiz/test
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/quiz/test
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 250m
              memory: 512Mi
