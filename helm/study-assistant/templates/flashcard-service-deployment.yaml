apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "study-assistant.fullname" . }}-flashcard-service
  labels:
    {{- include "study-assistant.labels" . | nindent 4 }}
    app.kubernetes.io/component: flashcard-service
spec:
  replicas: {{ .Values.flashcardService.replicaCount }}
  selector:
    matchLabels:
      {{- include "study-assistant.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: flashcard-service
  template:
    metadata:
      labels:
        {{- include "study-assistant.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: flashcard-service
    spec:
      containers:
        - name: flashcard-service
          image: "{{ .Values.flashcardService.image.repository }}:{{ .Values.flashcardService.image.tag }}"
          imagePullPolicy: {{ .Values.flashcardService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.flashcardService.service.targetPort }}
              protocol: TCP
          env:
            {{- toYaml .Values.flashcardService.env | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 250m
              memory: 512Mi
