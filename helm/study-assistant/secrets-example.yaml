# Example secrets file - DO NOT commit real values to git!
# Copy this file to secrets.yaml and update with real values
# Add secrets.yaml to .gitignore

apiVersion: v1
kind: Secret
metadata:
  name: teamdeployaas-studyapp-study-assistant-secrets
  namespace: teamdeployaas-studyapp
type: Opaque
data:
  # Base64 encoded values - use: echo -n "your-value" | base64
  DATABASE_USERNAME: c3R1ZHlhc3Npc3RhbnQ=  # studyassistant
  DATABASE_PASSWORD: Y2hhbmdlbWUxMjM=      # changeme123
  JWT_SECRET: eW91ci1qd3Qtc2VjcmV0LWtleS1jaGFuZ2UtbWU=  # your-jwt-secret-key-change-me
  WEBUI_API_KEY: eW91ci13ZWJ1aS1hcGkta2V5  # your-webui-api-key

---
# To generate secure values, use these commands:
# For DATABASE_PASSWORD: echo -n "$(openssl rand -base64 32)" | base64
# For JWT_SECRET: echo -n "$(openssl rand -base64 64)" | base64  
# For WEBUI_API_KEY: echo -n "your-actual-api-key" | base64
